
* Para o back end:

```

> cd backend
> pip install virtualenv
> virtualenv venv
> source venv/bin/activate

```

* Para o front end:

```

> cd app
> npm install
> npm install ionic -g 
> ionic serve
